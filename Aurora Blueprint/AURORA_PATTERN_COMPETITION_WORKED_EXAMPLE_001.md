# AURORA PATTERN COMPETITION WORKED EXAMPLE 001

## PURPOSE

This file is an illustrative worked example for the pattern competition engine only.

It does not emit:
- a trade
- geometry
- an opportunity declaration
- a generated strategy card

It exists to show how Aurora can compare multiple pattern hypotheses honestly downstream of family competition while preserving ambiguity.

Cross-reference:
- `AURORA_PATTERN_COMPETITION_ENGINE_PROTOCOL.md`
- `AURORA_PATTERN_COMPETITION_OBJECT_SCHEMA.md`
- `AURORA_FAMILY_COMPETITION_WORKED_EXAMPLE_001.md`
- `AURORA_FAMILY_LANE_PACKET_002_C04_G4.md`
- `patterns/G4_Failed_Break_Reclaim.card.md`

---

# 1. SCENARIO POSTURE

- `worked_example_id`: `AURORA_PATTERN_COMPETITION_WORKED_EXAMPLE_001`
- `status`: `ILLUSTRATIVE_BUILD_PHASE_SPECIMEN`
- `symbol_posture`: `NON_DOCTRINAL_INSTANCE_SHELL`
- `family_neighborhood`: `C-04 / C-03`
- `pattern_neighborhood`: `G4 failed break / reclaim` versus unresolved breakout/reclaim ambiguity

Scenario shell:
- the upstream family competition result already returned `CONTESTED_PRIMARY`
- `C-04 Failed Break / Trap Reversal` leads, but `C-03 Breakout / Compression Release` remains materially alive
- visible reclaim back through prior structure exists
- the reclaim does not yet look strong enough to lock a full failed-break/reclaim confirmation cleanly
- execution and session truth remain partial

---

# 2. INPUT BASIS

## Source refs
- `family_competition_example_ref`: `AURORA_FAMILY_COMPETITION_WORKED_EXAMPLE_001.md`
- `family_competition_protocol_ref`: `AURORA_FAMILY_COMPETITION_ENGINE_PROTOCOL.md`
- `pattern_atlas_ref`: `AURORA_SETUP_PATTERN_ATLAS.md`
- `pattern_card_ref`: `patterns/G4_Failed_Break_Reclaim.card.md`

## Missing surfaces carried in
- `execution_continuity_state`: `PENDING`
- `session_quality_detail`: `DEGRADED`
- `reclaim_persistence_detail`: `DEGRADED`
- `renewed_acceptance_detail`: `PENDING`

## Why the input is still valid for pattern competition
- the case contains enough structural information to compare at least one concrete failed-break/reclaim child against a deferred unresolved alternative
- the case does not contain enough truth to justify downstream opportunity or geometry

---

# 3. INITIAL CANDIDATE GENERATION

Initial live pattern set:
- `G4 Failed Break / Reclaim`
- `UNRESOLVED_BREAKOUT_RECLAIM_TRANSITION`

Why each entered the set:
- `G4` entered because reclaim through initiating structure is visible and the parent-family lead remains failure-first
- `UNRESOLVED_BREAKOUT_RECLAIM_TRANSITION` entered because the reclaim is not yet strong enough to eliminate the possibility that the move is still transition-heavy rather than a clean G4 confirmation

Rejected at entry:
- no balance-rotation pattern is carried in because upstream family competition already rejected `C-05`

---

# 4. PATTERN-BY-PATTERN COMPARISON

## 4.1 `G4 Failed Break / Reclaim`
- `pattern_result_state`: `CANDIDATE`
- `parent_family_compatibility_note`: G4 fits the leading `C-04` family posture directly
- `structural_role_fit_note`: visible reclaim through prior structure supports the G4 role
- `anti_confusion_pressure_note`: unresolved breakout continuation and ordinary pullback remain meaningful anti-confusions
- `state_alignment_note`: market-state interpretation still favors failure becoming clearer than continuation
- `regime_compatibility_note`: regime remains failure-first, but not clean enough for full confirmation
- `contradiction_load_note`: moderate contradiction load because the reclaim may still prove temporary
- `dependence_on_missing_surfaces`: reclaim persistence detail and renewed acceptance detail would materially change the result
- `evidence_not_yet_present`: stronger proof that the reclaim is holding rather than merely revisiting structure
- `deployability_compatibility_note`: deployability incompleteness does not invalidate the pattern thesis
- `reason_summary`: G4 leads the pattern set, but only at candidate level

## 4.2 `UNRESOLVED_BREAKOUT_RECLAIM_TRANSITION`
- `pattern_result_state`: `UNRESOLVED`
- `parent_family_compatibility_note`: this pattern posture exists because upstream family competition was contested rather than fully resolved
- `structural_role_fit_note`: the case still looks transition-heavy and does not yet cleanly eliminate breakout restoration
- `anti_confusion_pressure_note`: the anti-confusion is exactly the risk of mistaking a temporary reclaim for a fully failed break
- `state_alignment_note`: state alignment is weaker than G4 but still materially alive
- `regime_compatibility_note`: the regime can still shift back toward release if the reclaim weakens
- `contradiction_load_note`: contradiction load is moderate because failure-first structure currently leads
- `dependence_on_missing_surfaces`: renewed acceptance detail matters most here
- `evidence_not_yet_present`: clearer evidence that the reclaim is weak, late, or not holding
- `deployability_compatibility_note`: not decisive here
- `reason_summary`: unresolved transition remains alive because the local structure is not yet clean enough for hard G4 confirmation

---

# 5. PATTERN COMPETITION OUTCOME

- `pattern_competition_status`: `CONTESTED_PATTERN_PRIMARY`
- `confidence_posture`: `PARTIAL`
- `primary_pattern_candidate`: `G4 Failed Break / Reclaim`
- `surviving_patterns`:
  - `G4 Failed Break / Reclaim`
  - `UNRESOLVED_BREAKOUT_RECLAIM_TRANSITION`
- `rejected_patterns`:
  - `NONE`

## Why the result is not `CLEAR_PATTERN_PRIMARY`
- reclaim persistence detail is still missing
- renewed acceptance detail is still missing
- the upstream family result was contested rather than fully clear

## Why the result is not `MULTIPLE_LIVE_PATTERNS`
- G4 still has a cleaner current explanatory edge than the unresolved transition posture

---

# 6. WHAT WOULD CHANGE THE RESULT

## What would strengthen `G4`
- stronger reclaim persistence through the prior structure
- further failure of renewed breakout attempts
- evidence that continuation authority keeps degrading locally

## What would invalidate `G4`
- renewed acceptance beyond the original break area
- loss of the reclaim hold
- evidence that the reclaim was only temporary excursion

## What would reduce ambiguity
- clearer session-quality support during the reclaim window
- cleaner execution continuity during the local transition

---

# 7. WHY THIS EXAMPLE MATTERS

This worked example shows the intended pattern competition behavior:
- pattern competition stays downstream of family competition
- one pattern can lead without being fully confirmed
- ambiguity can remain visible
- unresolved transition pressure can survive honestly
- downstream layers remain untouched until pattern competition is complete

---

# 8. CURRENT JUDGMENT

Aurora now has a dedicated worked example for pattern competition that reuses the active C-04 / G4 lane and preserves unresolved local structure honestly instead of forcing a clean pattern winner too early.
