# ASC Layers

This domain owns the processing layers:
- open/closed state
- open-symbol snapshot
- filter
- top-list selection
- deep selective analysis

Each layer owns its own meaning.
Do not collapse all layer logic into one giant function.
